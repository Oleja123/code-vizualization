# Server configuration
server.port=8081
spring.application.name=flowchart-visualizer

# JSON configuration
spring.jackson.serialization.indent-output=true
spring.jackson.default-property-inclusion=non_null

# AST Service (semantic-analyzer) configuration
ast.service.url=http://localhost:8080

