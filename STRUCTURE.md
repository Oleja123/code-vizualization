# 📂 Структура проекта Flowchart Visualizer

## 🎯 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    ТВОЁ ВЕБ-ПРИЛОЖЕНИЕ                       │
│                                                              │
│  ┌──────────────┐                    ┌──────────────┐       │
│  │  Браузер     │                    │   Go сервис  │       │
│  │  (HTML/JS)   │◄──────SVG──────────┤  CST-to-AST  │       │
│  └──────┬───────┘                    └──────┬───────┘       │
│         │                                    │               │
│         │ POST /api/flowchart/generate       │               │
│         │ { ast: {...} }                     │               │
│         ▼                                    │               │
│  ┌──────────────────────────────────┐       │               │
│  │   Java Flowchart Visualizer      │◄──────┘ AST JSON      │
│  │   (Spring Boot REST API)          │                       │
│  │                                   │                       │
│  │  1. Парсит AST                    │                       │
│  │  2. Строит граф блок-схемы        │                       │
│  │  3. Рендерит в SVG                │                       │
│  └──────────────────────────────────┘                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Файловая структура

```
flowchart-visualizer/
│
├── 📄 pom.xml                    # Maven конфигурация
├── 📄 README.md                  # Полная документация
├── 📄 QUICKSTART.md              # Быстрый старт
├── 📄 .gitignore                 # Git ignore
│
├── 📂 src/main/
│   │
│   ├── 📂 java/flowchart/
│   │   │
│   │   ├── 📄 FlowchartVisualizerApplication.java  # Spring Boot main
│   │   ├── 📄 FlowchartGenerator.java              # Главный API класс
│   │   │
│   │   ├── 📂 model/                # Модель блоков блок-схемы
│   │   │   ├── FlowchartNode.java       # Базовый класс узла
│   │   │   ├── Nodes.java               # Все типы узлов (Терминатор, Процесс, Решение...)
│   │   │   └── Location.java            # Позиция в исходном коде
│   │   │
│   │   ├── 📂 ast/                  # Модель AST (для парсинга JSON)
│   │   │   └── ASTModel.java            # Все AST классы (Program, Statement, Expression)
│   │   │
│   │   ├── 📂 builder/              # Построитель блок-схемы
│   │   │   └── FlowchartBuilder.java    # AST → Flowchart граф
│   │   │
│   │   ├── 📂 renderer/             # Рендеринг в SVG
│   │   │   └── SVGRenderer.java         # Flowchart → SVG по ГОСТ
│   │   │
│   │   └── 📂 api/                  # REST API контроллеры
│   │       └── FlowchartController.java  # POST /api/flowchart/generate
│   │
│   └── 📂 resources/
│       └── application.properties   # Конфигурация Spring Boot
│
└── 📂 demo/
    └── 📄 index.html               # Интерактивное демо (веб-интерфейс)
```

---

## 🔄 Поток данных

### 1️⃣ От кода к блок-схеме

```
Исходный C код
      ↓
┌─────────────────┐
│  Go CST-to-AST  │  conv.ParseToAST(code)
│     Service     │
└────────┬────────┘
         │
         ▼
    AST (JSON)
    {
      "type": "Program",
      "declarations": [...]
    }
         │
         ▼
┌─────────────────┐
│  Java Service   │  generator.generateSVG(ast)
│  FlowchartGen   │
└────────┬────────┘
         │
         ▼
    SVG String
    <svg>...</svg>
         │
         ▼
┌─────────────────┐
│   Браузер       │  document.innerHTML = svg
│   (HTML/JS)     │
└─────────────────┘
```

### 2️⃣ Внутри Java сервиса

```
AST JSON
   ↓
┌──────────────────────┐
│  Jackson Parser      │  ObjectMapper.readValue()
│  (JSON → Objects)    │
└──────────┬───────────┘
           │
           ▼
   Program объект
   (AST модель)
           │
           ▼
┌──────────────────────┐
│  FlowchartBuilder    │  buildFromProgram()
│  (AST → Graph)       │
└──────────┬───────────┘
           │
           ▼
   FlowchartNode граф
   (связанные узлы)
           │
           ▼
┌──────────────────────┐
│  SVGRenderer         │  render()
│  (Graph → SVG)       │
└──────────┬───────────┘
           │
           ▼
      SVG String
```

---

## 🎨 Классы блоков (ГОСТ 19.701-90)

| Класс | ГОСТ форма | AST источник |
|-------|-----------|--------------|
| **TerminalNode** | Скруглённый прямоугольник | FunctionDecl (начало), ReturnStmt (конец) |
| **ProcessNode** | Прямоугольник | VariableDecl, ExprStmt, AssignmentExpr |
| **DecisionNode** | Ромб | IfStmt, условия |
| **LoopStartNode** | Шестиугольник (верх) | WhileStmt, ForStmt |
| **LoopEndNode** | Шестиугольник (низ) | Конец цикла (возврат) |
| **ConnectorNode** | Круг | BreakStmt, ContinueStmt |

---

## 🔌 REST API Endpoints

### **POST** `/api/flowchart/generate`

Генерирует SVG из AST

**Request:**
```json
{
  "ast": {
    "type": "Program",
    "declarations": [...]
  }
}
```

**Response:**
```json
{
  "svg": "<svg xmlns='http://www.w3.org/2000/svg'>...</svg>",
  "metadata": {
    "success": true,
    "svgLength": 4521
  }
}
```

### **GET** `/api/flowchart/health`

Проверка работы сервиса

**Response:**
```json
{
  "status": "ok",
  "service": "Flowchart Visualizer",
  "version": "1.0.0"
}
```

---

## 🧩 Ключевые компоненты

### 1. **ASTModel.java** (парсинг JSON)

```java
// Автоматическая десериализация JSON в Java объекты
Program program = objectMapper.readValue(astJson, Program.class);
```

Содержит все AST классы:
- `Program`, `Statement`, `Expression`
- `VariableDecl`, `FunctionDecl`, `IfStmt`, `WhileStmt`, `ForStmt`...
- `BinaryExpr`, `UnaryExpr`, `AssignmentExpr`, `CallExpr`...

### 2. **FlowchartBuilder.java** (AST → граф)

```java
// Конвертирует AST в связанный граф блоков
FlowchartNode root = builder.buildFromProgram(program);
```

Основные методы:
- `buildFromProgram()` - точка входа
- `buildFunction()` - функция → блоки
- `buildStatement()` - statement → узел
- `buildIfStmt()` - if/else → DecisionNode
- `buildWhileStmt()` - while → LoopStartNode

### 3. **SVGRenderer.java** (граф → SVG)

```java
// Генерирует SVG строку из графа
String svg = renderer.render(root);
```

Основные методы:
- `render()` - главный метод
- `renderTerminal()` - скруглённый прямоугольник
- `renderProcess()` - прямоугольник
- `renderDecision()` - ромб
- `renderLoopStart()` - шестиугольник
- `drawArrow()` - стрелки между блоками

### 4. **FlowchartController.java** (REST API)

```java
@PostMapping("/generate")
public ResponseEntity<Map<String, Object>> generateFlowchart(
    @RequestBody Map<String, Object> request
) {
    String svg = generator.generateSVG(astJson);
    return ResponseEntity.ok(createResponse(svg));
}
```

---

## 🚀 Запуск и использование

### Для разработки:

```bash
# 1. Собрать проект
mvn clean install

# 2. Запустить сервер
mvn spring-boot:run

# 3. Проверить работу
curl http://localhost:8080/api/flowchart/health

# 4. Открыть демо
open demo/index.html
```

### Для продакшена:

```bash
# Собрать JAR
mvn clean package

# Запустить JAR
java -jar target/flowchart-visualizer-1.0.0.jar
```

---

## 🔮 Расширение для трассировки

Каждый SVG узел имеет:

1. **Уникальный ID** - `<g id="uuid">`
2. **Location** - связь с исходным кодом

Для пошаговой визуализации:

```javascript
// 1. Получить SVG
const svg = await generateFlowchart(ast);
document.getElementById('flowchart').innerHTML = svg;

// 2. При выполнении кода - подсветить блок
function step(nodeId) {
  // Убрать старую подсветку
  document.querySelectorAll('.active').forEach(n => 
    n.classList.remove('active')
  );
  
  // Подсветить текущий
  document.getElementById(nodeId).classList.add('active');
}

// 3. CSS для подсветки (уже есть в demo)
.active {
  fill: #ffeb3b !important;
  stroke: #f57c00 !important;
  stroke-width: 3 !important;
}
```

---

## 📦 Зависимости

### Maven (pom.xml):

- **Spring Boot Web** - REST API
- **Jackson** - JSON парсинг
- **Lombok** (опционально) - уменьшение boilerplate

### Версии:

- Java: 17+
- Spring Boot: 3.2.0
- Maven: 3.6+

---

## ✨ Итого

**Созданные файлы:**
- 10 Java классов
- 1 REST контроллер
- 1 HTML демо
- 2 документации (README + QUICKSTART)
- Maven конфигурация

**Что получилось:**
- ✅ Парсинг AST от Go сервиса
- ✅ Построение блок-схем по ГОСТ
- ✅ REST API для веб-приложения
- ✅ SVG с ID для трассировки
- ✅ Интерактивное демо

**Готово к:**
- Интеграции с твоим веб-приложением
- Добавлению пошаговой трассировки
- Расширению функционала

---

Удачи с проектом! 🚀
