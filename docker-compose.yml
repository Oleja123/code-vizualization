services:
  semantic-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    restart: unless-stopped

  interpreter:
    build:
      context: .
      dockerfile: interpreter-service/Dockerfile
    ports:
      - "8084:8084"
    restart: unless-stopped
